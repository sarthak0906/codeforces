#include<bits/stdc++.h>

using namespace std;

typedef long long int ll;

int main(){
	int n, m;
	cin >> n >> m;
	int last = 0;
	queue
	for (int i=0;i<n;++i){
		int a;
		cin >> a;
		if (a > m)
	}
	if (last) cout << last+1;
	else cout << n ;
	return 0;
}

